# 算法小样本快速升级迭代训练软件

## 安装
conda create --name afruit python=3.11 -y
conda activate afruit
bash install.sh

## 使用说明
功能代码在utils里
封装的service和api在core里
测试代码在tests里，直接执行python test_xxx.py就可以
主程序在main.py，直接执行python main.py就可以，有一个可视化界面

